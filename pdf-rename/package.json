{
  "name": "pdf-rename",
  "version": "0.1.0",
  "private": true,
  "description": "Extract Acrobat-named PDF form fields, propose snake_case names, and apply a reviewed mapping back to the PDF.",
  "type": "module",
  "scripts": {
    "extract": "tsx src/extract.ts",
    "apply": "tsx src/apply.ts",
    "verify": "tsx src/verify.ts"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.7.76"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
